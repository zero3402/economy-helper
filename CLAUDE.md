# economy-helper — 무엇을 만드는가

재테크에 필요한 값(환율·주식·코인·뉴스·날씨)을 **텔레그램으로 밀어 주고, 물으면 답하는** 봇.

**시니어 개발.**

문서가 셋이고 역할이 갈린다. 겹치면 한쪽만 낡는다.

| 문서 | 맡는 것 |
|---|---|
| `CLAUDE.md` (이 문서) | **무엇을** 만드는가 — 요구사항과 출처별 결정 |
| `ARCHITECTURE.md` | **왜 이렇게** 만들었는가 — 구조·프로세스·트레이드오프 |
| `DEPLOY.md` | **어디에** 올리는가 — 배포처·환경변수·웹훅 등록 |

---

## 구성

| 항목 | 선택 |
|---|---|
| 런타임 | Spring Boot 4 (JDK 21, 가상 스레드) |
| 저장소 | Redis — 캐시·락·발송이력·토큰·쿼터를 한 곳에서 |
| 배포 | Render · GitHub Actions |
| 오케스트레이션 | k8s |
| 창구 | 텔레그램 봇 + 웹사이트 |

**HA 구성**: 이중화 · 서킷브레이커 · 캐시 · 락

**디렉터리**: `backend/` · `frontend/`

### 전제 조건

- **반드시 테스트를 넘겨야 다음 것을 진행한다.**
- 외부 API는 Rate Limit·응답 에러 처리를 주의해서 연동한다.
- `.env`에 API 비밀키가 있다. **절대 노출되면 안 된다.**

---

## 기능

### 0. 프론트엔드 *(미구현)*

- **0-1** API 중심으로 Vue.js 프론트엔드도 제공하며 Cloud Pages로 배포
- **0-2** 구글 애드센스도 붙임

### 1-1. 아침 브리핑 — 매일 오전 9시

텔레그램으로 아래를 보낸다.

- 환율
- 유명 주식 가격
- 유명 코인 가격
- 재테크에 꼭 필요한 뉴스를 뽑아 각각 1개 (한글 요약)

### 2. `/news {검색어}` *(줄임말 `/n`)*

검색어에 해당하는 오늘 기사 중 **점수 상위 3건**. 제목과 본문을 한글로 옮겨 함께 준다.

> ⚠️ **「조회수 또는 댓글 순위」는 얻을 수 없다.** 어느 매체도 그 숫자를 공개하지 않는다
> (Most Read 페이지도 전부 403/404였다). 그래서 `PopularityScorer`가 공개 신호 넷으로
> 근사한다 — 피드 순위·최신성·검색어 일치·HN 반응. 그중 HN 반응만이 실측된 남의 반응이다.
>
> ⚠️ **요약하지 않는다.** 번역 프롬프트가 「요약하거나 줄이지 말고 원문의 내용을 그대로
> 옮기세요」라고 못 박는다 — 요약은 우리가 기사를 다시 쓰는 일이고, 틀리면 우리 탓이다.
> 그래서 「한글 5줄 요약」이 아니라 **한글 번역**이다.

### 3. `/fx`

원/달러 환율(KRW-USD).

### 4. `/stock {검색어}` *(줄임말 `/s`)*

검색어에 대응되는 현재 주가. **목표주가와 실적발표일을 함께 적는다**(검색과 브리핑 둘 다).

```
국내                     미국

삼성전자                 애플
239,500 KRW             232.14 USD
🔵 -1.26%               327,821 KRW
                        🔵 -1.75%
목표가                   목표가
466,667 KRW             340.72 USD
                        481,155 KRW

                        실적발표일(미국)
                        2026.10.29(목)
```

*(위 둘은 실제로는 한 통 안에서 「국내」·「미국」 머리를 달고 위아래로 쌓인다 — 나란히 둔 것은
같은 자리에 무엇이 채워지고 무엇이 비는지 견주려는 것이다.)*

**시장에 따라 채워지는 칸이 다르다.** 실적발표일은 **미국에만** 있다 — 국내에 무료 출처가 없다.

| 값 | 국내 | 미국 |
|---|---|---|
| 목표주가 | KIS `invest-opinion` | FMP `price-target-consensus` |
| **실적발표일** | **없다** | FMP `/stable/earnings` |

> **목표가도 값과 같은 규칙으로 환산한다.** 달러 목표가 아래에 원화가 한 줄 따라붙는다 —
> 값 줄이 `232.14 USD` / `327,821 KRW`인데 목표가만 한 통화면 읽는 사람이 손으로 곱해야 한다.
> **국내에는 안 붙는다**(이미 원화다). 그 줄이 없던 동안 화면이 값 줄의 규칙에서 혼자 빠져 있었다.
>
> ⚠️ **값 줄이 쓰는 그 환율이어야 한다.** 둘이 다른 고시를 쓰면 같은 통에 두 환율이 찍혀
> 어느 쪽도 못 믿게 된다 — 브리핑이 환율을 한 번만 조회해 증시 통까지 들고 가는 이유가 이것이다.
> 환율을 못 구하면 **환산 줄만 빠지고** 달러 목표가는 그대로 나간다.

- **둘이 따로 논다** — 목표주가와 실적발표일은 출처가 갈리고 어떤 것은 아예 없다.
  **없는 것은 줄이 아예 없다.** `0`이나 「-」로 찍으면 그건 모른다는 뜻이 아니라 **값**이다.
- **이름표가 블록의 머리다.** 시세 줄들은 이름표가 없어(`311.30 USD`·`🔵 -1.75%`) 줄만 바꿔도
  무엇인지 읽히지만, 전망은 **이름표를 달아야 뜻이 서는 값**이다. 그 이름표를 앞줄에 바싹
  붙이면 위 숫자 무리에 딸려 붙어 읽히므로 **앞에 빈 줄을 둔다.** 값은 이름표 바로 아랫줄이다 —
  한 블록 안이라 「빈 줄은 블록 사이, 한 줄은 블록 안」 규칙 그대로다. 대가는 줄 수인데
  (종목마다 최대 여섯 줄) 한 통 상한 4,096자에는 한참 못 미친다 — **실측 410자**다
  (지수 넷·종목 셋에 전망 셋을 다 붙인 브리핑 증시 통).
- **지수에는 안 붙인다** — 코스피에 목표주가를 낼 주체가 없다.
- **ETF에는 대개 빠진다** — 목표주가와 실적발표일은 증권사·기업이 **기업**에 대해 내는 값이다.
  `/stock SCHD`에 그 줄이 없는 것이 정상이고, 「없는 것은 줄이 아예 없다」 규칙 그대로다.
  ⚠️ 다만 **FMP 전망 엔드포인트를 ETF로 실측하지는 않았다** — 허용목록이 따로일 수 있어
  402인지 빈손인지 모른다. 어느 쪽이든 시세는 나간다(KIS 시세는 「주식」 절에서 실측했다).
- **보충이지 폴백이 아니다.** 전망 조회가 실패해도 시세는 그대로 나간다.

> **실적발표일에는 이름표가 기준을 든다 — `실적발표일(미국)`.** 값이 **일 단위**라
> 화면만 보고는 어느 달력의 그날인지 알 수 없는데, 그 날짜는 **미국 거래일 그대로**다(KST가 아니다).
> 환산하지 않는 이유는 발표가 미국 장 마감 뒤라 KST로 옮기면 대개 **다음 날 새벽**이 되어
> 「그 시장의 달력」이 깨지기 때문이다. 환율의 `(고시)`, 증시의 `(종가)`, 날씨의 `(예보)`와
> 같은 자리다.
>
> ⚠️ **꼬리표는 그 자체로 읽혀야 하고, 그 이상은 군더더기다.** 이 한 줄에서 두 번 틀렸다.
> `(현지)`만 적었을 때는 **어디 현지인지를 말하지 않아** 모자랐다 — 무리 제목의 「미국」이
> 위에 있긴 하나 그 사이에 이름·값·환산·등락률·목표가 블록이 끼어 있어, 이 줄에 닿을 때쯤
> 그 머리와 연결이 끊긴다. 그렇다고 `(미국 현지)`로 적으면 **나라를 적은 뒤에도 「현지」가
> 남아** 넘친다. 가운데가 `(미국)`이고, 괄호 앞 공백도 두지 않는다 — 이름표는 한 낱말로 읽힌다.
>
> **꼬리표를 값 줄이 아니라 이름표에 단다.** 이 통은 「값 줄에는 꼬리표를 붙이지 않는다」가
> 규칙이고, 이름표가 이미 블록의 머리이므로 성격도 거기가 제자리다.
>
> **날짜는 통의 다른 날짜와 같은 모양이다** — `2026.10.29(목)`(`MessageLayout.DATE`).
> 이 줄만 `10월 29일(목)`로 갈라 뒀던 때가 있는데, 한 통에 두 표기가 섞이는 대가를 치를 만큼
> 성격 차이가 크지 않았다. 연도가 있어야 **다음 분기인지 내년인지**도 읽힌다.

> ⚠️ **투자의견은 걷어냈다.** 목표가 아래에 「매수 (111곳)」 줄이 있었다. 요구가 바뀌면서
> **화면만 지우지 않고 값과 호출까지 함께 지웠다** — `StockOutlook`의 두 필드(`rating`·
> `analystCount`), 국내의 등급 정규화 표, 그리고 FMP `grades-consensus` 호출이다.
> 화면에서만 빼면 심볼당 하루 한 번을 **아무도 안 보는 값**에 쓴다.
>
> 다시 넣을 일이 있으면 KIS의 `invt_opnn_cls_code`를 등급으로 쓰지 말 것 — 실측(5종목 378행)에서
> 코드 하나에 `Strong BUY`·`Hold`·`Outperform`·`Buy`가 함께 있었고, 같은 응답 안에서 표기도
> 갈렸다(`BUY` 키움·삼성 / `매수` 한국투자). 코드로 읽으면 **매도가 조용히 중립이 된다.**

> **국내는 우리가 접고, 미국은 이미 접혀 온다.** KIS `invest-opinion`은 컨센서스가 아니라
> **발표 건**을 준다(실측 2026-08-21: 삼성전자 두 달에 12행). 그래서 `InvestOpinions`가
> **증권사별 최신 하나**만 남겨 평균 목표가를 만든다 — FMP가 컨센서스로 이미 합쳐 주는 값을
> 국내에서는 우리가 만드는 셈이고, 그래야 두 시장의 숫자가 같은 뜻이 된다.
>
> ⚠️ **목표가 `0`은 값이 아니다.** 목표가를 안 낸 발표가 `0`으로 오는데 평균에 넣으면 실제보다
> 낮은 목표가가 나간다 — 시세에서 `Price.positive`가 막는 그 함정과 같다.
>
> **미국은 심볼당 FMP 호출이 둘이다**(`price-target-consensus`·`earnings`, 실측 2026-08-21
> 둘 다 200). 무료 배치가 막혀 있어 합칠 수 없고, 하루 250회에서 심볼 하나가 2회를 쓰므로
> **12시간 캐시가 한도의 실질 방어**다. 무료 티어가 심볼별 허용목록이라 하나만 402일 수
> 있는데, 그때 **살아 있는 것은 살린다** — 목표가만 있거나 실적발표일만 있는 답이 정상이고,
> **둘 다 실패했을 때만** 던진다.
> `targetHigh`가 아니라 `targetConsensus`를 쓴다(실측 `AAPL`: 고가 400 · 컨센서스 340.72).
>
> ⚠️ **브리핑도 이 값을 받는다.** 예전에는 브리핑이 미국 시세만 받아 전망이 `/stock` 검색에만
> 나왔는데, 요구는 「검색 **및 알림** 때 보여 준다」였다. 브리핑의 미국 **종목**은 둘
> (엔비디아·애플)이라 하루 4회이고, 12시간 캐시라 그 사이 검색이 호출을 나눠 쓴다.
>
> **실적발표일은 FMP `/stable/earnings`가 준다**(실측 2026-08-21 `AAPL` 200).
> 목표가 엔드포인트가 주지 않아 **별도 호출**이고, 그래서 심볼당 FMP 호출이 둘이다.
>
> ⚠️ **첫 행을 그냥 집지 않는다.** 지난 분기가 함께 오고 최신순인 것처럼 보이지만(실측:
> `2026-10-29`·`2026-07-30`·`2026-04-30`), 정렬을 응답이 지켜 준다는 보장이 없다 —
> 한 번 어긋나면 「다음 발표」 자리에 **지난 분기**가 적힌다. **오늘 이후만 남기고 가장 이른 것**을 고른다.
>
> ⚠️ **오늘을 미국 달력으로 자른다.** FMP가 주는 것은 미국 거래일이라 KST로 자르면 하루가
> 어긋나 오전 9시 브리핑이 어제(현지) 발표를 「예정」이라 적는다 — 날씨가 세운
> 「조회한 지역의 현지 달력으로 자른다」와 같은 규칙이다.
>
> ⚠️ **`epsActual`이 비었는지로 가르지 않는다.** 발표 후 FMP가 그 칸을 늦게 채울 수 있어
> 달력이 더 믿을 만하다.
>
> ⚠️ **국내에는 이 값이 없다.** KIS `invest-opinion`에 그 필드가 아예 없고, 무료 대안을 찾지
> 못했다. 그 칸은 `null`로 남고 화면은 **그 줄을 안 적는다** — 없는 것을 지어내지 않는다.

> ⚠️ **한투에는 「해외 목표주가」가 없다 — 찾아봤고 없었다.** 국내 목표가를 주는
> `invest-opinion`(`FHKST663300C0`)은 `FID_COND_MRKT_DIV_CODE=J`라 **KRX 전용**이고,
> 대응하는 해외 엔드포인트가 존재하지 않는다. 근거 둘(2026-08-21):
>
> 1. 공식 저장소 `koreainvestment/open-trading-api`의 **해외주식 함수 46개 전체**에
>    `투자의견`·`목표`·`추정`·`실적`·`analyst`·`target`·`estimate`·`earning`이 **한 건도 없다.**
>    시세 계열은 `price`·`price_detail`·`dailyprice`·`inquire_daily_chartprice`·
>    `inquire_asking_price`·`search_info`·`inquire_search`뿐이고 나머지는 순위·뉴스·권리·주문·잔고다.
> 2. 이름에 없어도 필드에 숨어 있을 수 있으므로 **본문을 통째로 떴다.** `price-detail`이
>    `PATH`에 주는 43개 필드는 시세(`last`·`base`·`uplp`·`dnlp`)·52주(`h52p`·`l52p`)·
>    펀더멘털(`perx`·`pbrx`·`epsx`·`bpsx`·`shar`·`mcap`)·원화환산(`t_xprc`…)·
>    기타(`e_icod`·`e_parp`·`etyp_nm`)다 — **목표주가도 실적발표일도 없다.**
>
> `search-info`(`CTPF1702R`)만 **모의에서 못 쟀다**(`EGW02006` 「모의투자 TR 이 아닙니다」).
> 이름이 「상품기본정보」라 종목 메타일 것으로 보지만 **그건 추정이고 실측이 아니다** —
> 실전으로 옮기는 날 이것 하나는 다시 볼 것.
>
> **그래서 미국 목표가·실적발표일의 2순위는 한투가 될 수 없다.** 붙이려면 새 출처가 필요하고,
> 그건 새 키와 새 의존이 함께 온다.

### 4-1. 일봉 차트 — 환율·주식·코인

`/fx`·`/stock`·`/crypto` 답과 아침 브리핑에 **최근 14 거래일 차트를 사진으로** 함께 보낸다.
브리핑은 **종목마다 한 장씩 순차로** 나간다(같은 방에 초당 한 통을 지킨다).

> **그림에는 글자를 넣지 않는다.** 런타임 이미지가 `eclipse-temurin:21-jre`이고 **폰트 패키지를
> 안 깐다** — 글자를 그리면 배포처에서 두부(□□□)가 될 수 있고, 개발 기계에 폰트가 있다는 것은
> 아무 증거가 아니다. 그래서 그림은 **기하(선·기준선)만** 그리고 낱말과 숫자는 전부 caption에 둔다.
>
> 얻는 것이 셋이다. 폰트 의존이 **0**이 되고, caption이 텍스트라 **골든이 낱말 전부를 계속
> 덮고**(그림은 골든이 못 본다), 그림은 결정적 기하라서 성질로 검증할 수 있다.
> **폰트를 깔아서 해결하지 않는다** — 문서 어디에도 없는 숨은 의존이 새로 생긴다.
>
> 숫자 없이 방향을 읽게 한다: **첫날 값에 가로 기준선**을 긋고, 선 색을 등락률 이모지와 같은
> 방향으로 갈랐다(오르면 붉고 내리면 푸르다). 새 의존성은 없다 — JDK의 `Graphics2D`다.
>
> ⚠️ **caption 상한은 4096이 아니라 1024다.** 메시지 상한으로 자르면 400을 맞아 **사진이
> 통째로 안 나간다** — 「일부라도 보내려던 것이 전부를 잃는」 그 함정을 상한만 바꿔 다시 밟는 셈이다.
>
> ⚠️ **일봉을 시세 캐시에 합치지 않는다.** 환율과 주식은 일봉이 **이미 시세 응답에 온다**
> (Frankfurter는 10일 시계열을 받아 두 점만 썼고, KIS `output2`는 읽지도 않았다). 그래도 합치지
> 않는 이유는 수명이다 — 시세는 1분이고 일봉은 하루에 한 번 바뀌어서, 한 항목에 담으면 **하루치
> 값을 1분마다 다시 받는다**(60배). 게다가 시세 캐시는 타입이 못 박혀 있어 다른 모양을 담으면
> 쓸 때는 넘어가고 읽을 때 깨진다.
>
> ⚠️ **차트가 없는 자리가 있고, 그것이 정상이다.** **그 종목만 차트가 빠지고 값은 그대로
> 나간다** — 보충이지 폴백이 아니다.
>
> | 무엇 | 차트 | 왜 |
> |---|---|---|
> | 환율 · 코인 | 검색·브리핑 | |
> | 국내 종목 · 국내 지수 · 미국 종목 · 미국 지수 | 검색·브리핑 | 조회 열쇠를 `StockService.Answer`가 함께 들고 온다 |
> | **이름으로만 찾은 국내 종목** | **없다** | 2순위(공공데이터포털)는 이름으로 찾고 **종목코드를 안 준다** — 일봉을 물을 열쇠가 손에 없다. 전망이 빠지는 것과 같은 이유다 |
> | **어느 거래소에도 칸이 없는 미국 종목** | **없다** | NAS·NYS·AMS를 다 훑어도 `output2`가 비면 그릴 것이 없다 |
> | **업비트에 없는 코인** | **없다** | 바이낸스만 상장된 것(`BNB`)은 `market`이 `null`이라 일봉을 물을 곳이 없다 |
> | **날씨** | **없다** | 요청받은 적이 없다. 빠진 것이 아니라 안 만든 것이다 |
>
> ⚠️ **환율 차트에는 이중화가 없다.** 시세는 KIS → 유럽중앙은행 → 수출입은행 3단인데 일봉은
> **Frankfurter 하나**다(`FxService.dailyBars`). 그래서 **KIS가 답한 환율에 차트가 안 붙는
> 경우가 있다** — Frankfurter가 죽거나 브레이커가 열렸을 때다. 값은 나가고 그림만 빠지므로
> 틀린 동작은 아니지만, 「환율은 이중화 3단」만 읽으면 차트도 그럴 것이라 여기게 된다.
> **이중화되는 것은 시세다.**
>
> **지수 넷은 재료가 이미 있었다.** 지수 시세가 부르는 그 엔드포인트가 `output2`에 일자별
> 배열을 함께 주고(실측: 코스피 `bstp_nmix_prpr`, 나스닥 `ovrs_nmix_prpr`),
> `KisStockApi.Bar`가 세 시장 필드를 다 선언해 두고 있었다. **배선만 없었다** — 그리고
> 일봉에 테스트가 하나도 없어 아무도 그것을 알려 주지 않았다.
>
> ⚠️ **미국 종목은 종목 전용 경로로 간다**(`HHDFS76240000` · `/quotations/dailyprice`).
> **한 번 틀렸다가 고친 자리다.**
>
> 처음에는 미국 **지수** 일봉 경로(`FHKST03030100`)에 종목 심볼을 그냥 넣었다. `AAPL`·`NVDA`·
> `ORCL`이 200이길래(실측 2026-08-21, 셋 다 `rt_cd=0` · 20행) 「거래소 코드가 필요 없다」를
> 이득으로 셌는데, **그 경로가 아는 종목은 일부뿐이었다** — `PATH`는 `rt_cd=0`인데
> `output2`가 빈 배열이라 **주가는 나오고 차트만 조용히 빠졌다.**
>
> 반대 이유였던 `EXCD`는 **이미 손에 있다.** 시세(`price-detail`)가 NAS→NYS→AMS를 탐색해
> `KisExchangeCache`에 30일 담고, 차트는 시세 다음에 조회된다 — 평상시 추가 호출이 **0**이다.
> 못 찾으면 시세가 쓰는 그 목록으로 훑고 찾은 것을 기억한다(목록을 두 번 적지 않는다).
>
> | 심볼 | `EXCD` | `rsym` | 행 | 최근 종가 |
> |---|---|---|---|---|
> | `PATH` | NYS | `DNYSPATH` | 100 | 15.9200 |
> | `ORCL` | NYS | `DNYSORCL` | 100 | 143.3600 |
> | `AAPL` | NAS | `DNASAAPL` | 100 | 312.0600 |
>
> ⚠️ **`MODP=0`이다 — 수정주가를 쓰지 않는다.** 국내(`FID_ORG_ADJ_PRC=0`)와 반대이고,
> 값을 맞춰 보고 뒤집었다: `MODP=1`은 **가장 최근 행까지 조정 단위로 스케일한다**
> (실측 `AAPL`: `MODP=1`이 311.94인데 같은 순간 `price-detail`은 312.06). 그러면 차트의
> 오른쪽 끝이 「지금 얼마냐」가 아니게 되어 **한 종목의 값이 한 통에 두 개 찍힌다.**
> 창 안에 액면분할이 들어오면 원주가가 절벽을 그리는 것이 대가인데, 그건 드물고 어긋난
> 끝값은 매일 틀린다.
>
> **창을 우리가 정하지 않는다.** `BYMD`를 비우면 최신부터 100행이 오고 `DailySeries.recent`가
> 열나흘로 줄인다. 응답이 무거워지는 대신 날짜 계산이 없어진다.
>
> **지수 경로보다 하루 신선하다** — 오늘 진행 중인 거래일도 한 행으로 준다(실측 `20260821`).
> 그래서 브리핑에서 지수 차트와 종목 차트의 기간이 하루 어긋날 수 있는데, **버그가 아니라
> 각자 가진 것**이고 「오른쪽 끝이 지금」 규칙에는 이쪽이 더 맞는다.
>
> **지수는 옛 경로 그대로다.** 지수에는 거래소가 없어 종목 경로를 탈 수 없다.
>
> **지수 차트에는 단위를 붙이지 않는다.** 「6,869.83 KRW」라고 적을 근거가 없다 —
> 시세의 `Money.NONE`과 같은 판단이다.
>
> **코인만 진짜 새 호출이다**(업비트 `/v1/candles/days`). 키가 없고 한 호출로 열나흘을 준다.
> **바이낸스 klines는 쓰지 않는다** — 원화 시세는 업비트가 주므로 얻을 것이 없고, 그쪽은
> 밴 게이트 옆이라 호출을 늘리는 값이 다르다.
>
> ⚠️ **KIS는 「모른다」를 에러로 말하지 않는다 — 모양이 둘이다.** 어느 쪽도 `rt_cd`가 `0`이라
> 상태 코드만 보면 성공이다.
>
> | 모양 | 실측 | 우리가 하는 것 |
> |---|---|---|
> | 값이 `0.00`으로 온다 | 지수 경로의 `DJI`·`DJIA` | `DailySeries.recent`가 `signum() <= 0`을 걸러낸다 — 그대로 그리면 **차트가 0으로 절벽을 그린다** |
> | `output2`가 **빈 배열**이다 | 지수 경로의 `PATH`, 종목 경로의 **틀린 `EXCD`** | 종목은 다음 거래소로 넘어간다. 다 비면 던진다 |
>
> 둘 다 끝은 같다 — `drawable`이 거짓이라 **그 종목만 사진이 빠지고 값은 그대로 나간다.**
> 못 구한 날은 빈 칸으로 남긴다(0으로 채우지 않는다).
>
> ⚠️ **그 자리에 로그를 한 줄 남긴다.** 예전에는 조용히 `null`이어서 「KIS가 그 심볼을
> 모른다」와 「차트를 아예 안 물었다」가 로그에서 구분되지 않았다 — 「고쳤는데 여전히
> 안 나온다」의 정체를 찾을 단서가 없던 자리다.

### 5. `/crypto {검색어}`

검색어에 대응되는 현재 코인 시세. 거래소는 **업비트와 바이낸스 둘**이고 화면에 두 칸으로
나란히 적은 뒤 **김치 프리미엄**을 함께 낸다.

- 이름 매칭은 업비트 목록으로 한다(캐시된 목록에 대한 순수 계산이라 공짜다). 후보가 여럿이면
  **24시간 거래대금 1위**를 고른다 — 실측: `이더`가 6건(이더리움·이더리움클래식·이더파이·
  이더리움네임서비스·메가이더·에이더블유이)에 걸리고 이더리움이 2위의 60배다.
- **업비트에 아예 없는 코인만** LLM에게 티커를 묻는다(실측: 원화 마켓 283개에 `KRW-BNB`가 없다).
- **바이낸스 실패는 4xx라도 뜻이 다 다르다.** Render에서 실제로 온 것은 **418**이었다.

  | 코드 | 뜻 | 우리가 할 것 |
  |---|---|---|
  | 400 (`-1121`) | 없는 심볼 | 그 코인만 `미상장`. 브레이커는 이것만 무시한다 |
  | **418** | **429를 받고도 계속 불러 IP가 자동 밴됐다**(2분~3일) | **물러선다.** 브레이커를 열어 호출을 멈춘다 |
  | 429 | 밴 직전 경고 | 물러선다 |
  | 451 | 지역 차단 | 공개 미러로 한 번 더 묻는다 |

  ⚠️ **418에는 미러를 부르지 않는다.** 바이낸스 규칙상 밴 중의 호출이 **밴을 연장**하고,
  실측으로 두 호스트가 **한 IP 예산을 공유한다** — `api`↔`data-api`를 번갈아 물으니
  `x-mbx-used-weight-1m`이 2·4·6·8로 이어졌다. 우회해도 같은 밴을 받는다.

  ⚠️ **우리 사용량으로는 밴을 설명할 수 없다** — 조회당 1회, 가중치 2, 한도는 분당 1,200이다.
  남는 설명은 **Render의 공용 이그레스 IP**다. 한도가 IP 단위라 같은 NAT를 쓰는 다른 테넌트가
  태우면 우리 잘못 없이 함께 받는다. 그래서 우리가 할 수 있는 것은 **밴을 늘리지 않는 것**뿐이다.

  미러(`data-api.binance.vision`)는 **451 전용 보험**으로 남긴다 — 지역 규칙은 IP 예산과 달라
  호스트에 따라 갈릴 수 있다. 같은 `/api/v3` 스키마라 파싱이 안 갈린다(실측: 네 호스트가 같은 본문).

### 6. 날씨

- **6-1 알람** — 매일 오전 6시, 분당 미금역·서현역·잠실역·삼성중앙역
- **6-2 검색** — `/weather {검색어}`, 전 세계 모든 지역. 답은 **언제나 일일 예보**

> **하루를 오전 한 줄, 오후 한 줄로 적는다.** 「비옴」 한 마디로는 우산을 언제 챙길지 알 수 없다 —
> 실측(2026-08-20 성남시)으로 일 단위는 최대 강수확률 80%였는데 시간별로는 13~19시에 몰려 있고
> 오전은 말라 있었다. 사람이 하루를 계획하는 단위가 반나절이므로 그 둘을 각각 한 줄로 요약한다.
>
> ```
> ☁️ 오전 흐림
> ☔ 오후 1시~7시 비 (최대 80%)
> ```
>
> - **두 줄은 언제나 나온다.** 젖은 구간만 적으면 「오전」이 두 줄 나오거나 한 줄도 없는 날이
>   생겨, 읽는 사람이 나머지 반나절을 짐작하게 된다.
> - **반나절마다 한 줄이다.** 오전에 두 번 와도 **가장 센 토막** 하나만 적는다. 둘을 다 적으면
>   같은 접두사가 되풀이되고, 사이의 마른 시간까지 한 범위로 이으면 **오지 않는 비**를 적는 셈이다.
> - **비·눈이 없으면 그 시간대의 하늘을 있는 그대로 적는다.** 지어낸 「맑음」이 아니라 시간별
>   코드에서 읽은 값이다 — 그래야 하루 요약이 「흐림」인 날의 오전을 「맑음」이라 부르는 모순이
>   안 생긴다. **가장 흔한 코드**로 부른다: 대체로 맑은 오전에 이슬비 코드가 한 시간 낀다고
>   그 반나절이 「이슬비」가 되면, 문턱을 못 넘어 비로 안 치기로 한 값이 이름만 비가 된다.
> - **종류를 가른다** — 비·이슬비·소나기·눈·소낙눈·진눈깨비·뇌우, 그리고 마른 하늘.
>   그림도 갈린다(눈에도 마른 하늘에도 우산을 붙이지 않는다):
>   ☔ / ❄️ / 🌨️ / ⛈️ / ☀️ / 🌤️ / ⛅ / ☁️ / 🌫️
> - **시각에는 오전·오후를 안 붙인다.** 줄 앞의 반나절 이름이 이미 들고 있어서다 —
>   붙이면 `오후 오후 1시~7시`가 된다. **0시와 12시는 「12시」다**(오전 12시 · 오후 12시).
>   「정오」·「자정」은 쓰지 않는다 — 한 화면에 두 표기가 섞인다.
>
> ⚠️ **젖은 줄의 이름은 강수여야 한다.** 「비가 오는가」를 확률과 강수량만으로 정하면
> **강수가 아닌 낱말이 ☔ 줄에 앉는다** — 실측(2026-08-25 미금역)에서 10~12시가
> **강수확률 88~100%인데 WMO 코드는 `1`(대체로 맑음)이고 강수량은 `0.0mm`**였고, 그 시간이
> 젖은 것으로 잡혀 화면에 **`☁️ 오전 8시~11시 흐림 (최대 100%)`**가 찍혔다. 코드가 `0`인 날은
> 그대로 **「맑음」**이고, 그날 하루 요약이 「뇌우」면 **「뇌우인데 오전·오후 맑음」**이 된다.
>
> 그래서 규칙이 둘이다. **코드가 거부권을 쥐고**(`HalfDays.wet`), **이름은 강수 코드
> 중에서만 고른다**(`Stretch.extend`). 하나도 못 고르면 낱말 없이 시각만 적는다 —
> **줄까지 없애지는 않는다**(없애면 「반나절마다 반드시 한 줄」이 깨져 그 반나절이 마른 것처럼
> 읽힌다).
>
> ⚠️ **다만 코드 혼자서는 못 자른다.** 양이 잡힌 시간은 그 양이 이긴다 — 실측(2026-08-20
> 성남 17시)에 코드 `3`(흐림)인데 `0.2mm`가 함께 온 자리가 있었다. 「안 온다」고 말하려면
> **코드와 양이 함께** 말해야 한다. 그리고 **모르는 코드는 거부권이 없다**
> (`SkyCondition.UNKNOWN`) — 어휘를 못 읽었다고 시각 줄이 통째로 죽으면, 출처가 코드를
> 하나 늘리는 날 이 줄이 조용히 사라진다.
>
> ⚠️ **`null`은 `0`이 아니다.** 위 규칙을 적어 두고도 한동안 `amount == null`까지 「양이 안
> 온다고 말한다」로 셌다. 그래서 **강수량 배열이 없거나 짧게 온 응답**에서 확률 100%·코드
> `0`(맑음)인 시간이 전부 잘려 「맑음 / 강수확률 100% / ☀️ 오전 맑음 / ☀️ 오후 맑음」이
> 나왔다 — 제가 세운 규칙을 제가 어긴 것이다. 거부권은 **양이 있고 그 양이 문턱 아래일 때만**
> 선다. (적대적 리뷰가 잡았다.)
>
> ⚠️ **확률 `0%`도 「안 온다」는 값이다.** 같은 실측의 20시가 확률 `0%`에 `0.1mm`였는데,
> 양만 보고 비로 치면 화면이 **`(최대 0%)`**라고 적는다 — 우산 그림 옆의 0%는 제 말을 제가
> 뒤집는 일이다. 시세의 `Price.positive`·목표가 `0`과 같은 자리다.
>
> ⚠️ **쪼개는 것은 화면이 아니라 값에서 한다.** 반나절은 제 구간의 최대 확률을 드는데,
> 화면에서 한 토막을 두 줄로 그리면 두 줄에 같은 숫자가 찍혀 한쪽이 거짓이 된다.
> `HalfDays`가 접을 때 끊는다.
>
> ⚠️ **문턱은 반나절이 아니라 그날 봉우리로 잰다.** 반나절마다 다시 재면 오전 문턱이
> 내려가, 하루 기준으로는 걸러지던 시간대가 오전에만 새로 통과한다 — 같은 날 같은 비를
> 반나절에 따라 다르게 판정하게 된다. **끊는 것만 하고 문턱은 건드리지 않는다.**
> - **1순위(AccuWeather)는 시간 단위를 못 준다**(낮/밤 두 칸뿐). 그래서 시각만 Open-Meteo에
>   따로 묻는다 — 키도 한도도 없어 공짜다. 12시간별 엔드포인트가 있지만 12시간뿐이라
>   `내일 서현`·`일주일치 파리`를 못 덮고 하루 50회 예산을 반으로 줄인다.
>   **보충은 폴백이 아니라 덧붙임이라 실패해도 답을 죽이지 않는다** — 그때는 두 줄이 다 빠진다
> - 지나간 날은 확률이 없어 실제로 온 양으로 적는다(`☔ 오전 2시~4시 비 (3.7mm)`)
>
> ⚠️ **강수확률과 강수 시각은 반드시 한 출처에서 낸다.** 시간별을 보충으로 받으면 그날의
> **강수확률도 그 시간별 봉우리로 다시 센다**(`Weather.Daily.withPrecipitation`).
>
> 안 그러던 때가 있었고, 그것이 「**강수확률은 높은데 오전·오후 줄이 안 나온다**」의 정체였다 —
> 확률은 AccuWeather의 낮 값이고 시각은 Open-Meteo 시간별이라 **한 블록에 서로 다른 예보의
> 두 숫자**가 섰다. AccuWeather가 80%라고 하는 날 Open-Meteo 봉우리가 40%면 문턱
> (`max(50%, 봉우리×0.8)`)을 넘는 시간이 하나도 없어 **확률만 찍히고 시각 줄은 통째로 사라진다.**
> 골든의 `weather/four-places`(잠실역 80%, 시각 줄 없음)가 그 모양을 정상인 것처럼 담고 있었다.
>
> ⚠️ **그때 「확률이 50% 이상이면 반드시 시각 줄이 있다가 산술적으로 보장된다」고 적었는데,
> 그것은 과장이었다.** 문턱이 봉우리 이하라는 것만으로는 보장이 안 된다 — **봉우리 시각
> 자체가 코드·양 규칙에 걸려 빠질 수 있다.** 실측(2026-08-25 미금역)이 그 반례다:
> 확률 100%인 10~12시가 코드 `1`(대체로 맑음)에 `0.0mm`라 비로 치지 않으므로
> 「강수확률 100%」인데 오전에는 시각 줄이 없다. 보장되는 것은 **확률과 시각이 같은
> 시간별에서 나온다**는 것뿐이고, 그것만으로도 「두 예보의 숫자가 한 블록에 서는」 문제는
> 사라진다. (적대적 리뷰가 잡았다.)

> ⚠️ **그 보장에 구멍이 하나 있었다 — 반나절이 둘 다 마른 날이다.** 갈아 끼울 봉우리가 없어
> 일별 출처의 확률이 그대로 남았다: 실측(2026-08-26 미금역)이 **「소나기 / 강수확률 61% /
> ☁️ 오전 흐림 / ⛅ 오후 구름 조금」**이었다(61%는 AccuWeather 낮 칸, 반나절은 Open-Meteo
> 봉우리 18%). 그래서 **마른 반나절도 제 봉우리 확률을 든다**(`HalfDay.dry`) — 화면에는 안
> 적고(안 오는 비에 「최대 18%」를 붙이는 꼴이다) 하루 요약이 그것을 쓴다.
>
> ⚠️ **하루 요약과 반나절이 어긋나면 요약을 반나절 쪽으로 맞춘다**
> (`Weather.Daily.skyAgreeingWith`). 위 실측이 「흐림 / 강수확률 18%」가 되고, 반대로
> 「맑음」인데 반나절이 종일 비면 요약이 「비」로 올라간다.
>
> ⚠️ **「어긋난다」는 강수 여부가 갈리는 것이지 종류가 다른 것이 아니다.** 한동안 반나절이
> 있는 **모든** 예보 날에 요약을 갈아 끼웠는데, 실측(네 역 × 닷새)으로 **스무 날 중 16일이
> 바뀌고 그중 실제 모순은 4일뿐**이었다. 나머지 12일은 두 출처가 **둘 다 비라고 말하는데**
> 그냥 1순위를 버린 것이고, 그중 `소나기 → 이슬비`는 **경고를 깎는다** — AccuWeather가
> 1순위인 이유가 동 단위 지점 예보의 정확도인데 그 판단을 우리가 낮춰 적을 근거가 없다.
> **확률은 이미 시간별로 갈리므로**, 하늘까지 언제나 갈리면 1순위가 **기온 공급원**으로만
> 남는다 — 이중화의 전제가 조용히 무효가 된다. 좁힌 뒤 그 실측에서 바뀌는 것은 4일이다.
>
> ⚠️ **한동안 낮추는 쪽만 했다.** 근거는 「요약 『흐림』에 반나절 『☔ 오후 1시~7시 비』는
> 모순이 아니라 **『대체로 흐리고 한때 비』**로 읽힌다」였다. 그 해석 자체는 맞지만
> **모든 역방향에 통하지 않는다** — 「맑음 + 종일 비」는 어떻게 읽어도 모순이다. 한쪽만
>고치는 규칙은 예외를 정당화해야 하고 그 정당화가 한 입력에서 무너졌으므로, **양방향 한
> 규칙**으로 바꿨다. (적대적 리뷰가 잡았다.) 대가는 1순위(AccuWeather)의 하늘이 반나절에
> 밀리는 것인데, **확률은 이미 그렇게 하고 있었다** — 한 블록의 강수 값이 한 예보에서 나온다.
>
> ⚠️ **지나간 날은 건드리지 않는다.** 확률을 다시 세지 않는 날이 그것인데(재분석은 확률을
> 안 준다) 거기 담긴 것은 예보가 아니라 **실측**이다. 손댔더니 시간별이 전부 `0.1mm` 미만인
> 날에 **「맑음 / 강수량 0.1mm」**가 나왔다 — 비가 왔다고 적으면서 맑았다고 말하는 셈이고
> 「지난 날은 실제로 온 양으로 적는다」를 어긴다. 반나절 둘 다 어휘를 못 읽었으면 요약을
> 그대로 둔다 — **모르는 것으로 아는 것을 덮지 않는다.**
>
> ⚠️ **화면의 「강수확률」은 반나절 둘이 든 숫자의 최댓값이다** — 젖은 반나절은 제 토막의
> 봉우리를, 마른 반나절은 그 반나절의 봉우리를 든다. 그래서 **출처의 일별 최댓값보다 낮을 수
> 있다**: 봉우리 시각이 코드·양 규칙에 걸려 토막에서 빠지면 그 숫자가 안 올라온다
> (실측 2026-08-28 미금역: Open-Meteo 일별 35%, 화면 31%). **낮게 나올 뿐 높게 나오지는
> 않는다** — 없는 비를 부풀리는 쪽으로는 안 틀린다.
>
> **알람과 검색은 글자까지 같은 통이다.** 포매터도(`WeatherFormatter.format`) 서비스도
> (`WeatherService.forecast`) 하나이고 둘 다 오늘 하루라 1순위도 같다 — 다르게 보였다면
> 그것은 모양이 아니라 **값**이고, 위 두 함정이 그 정체였다.
> `WeatherDigestJobTest.rendersExactlyLikeSearch`가 그 동일함을 못 박는다.
>
> ⚠️ **대가는 출처 줄이 두 줄이 되는 것이다.** 강수 줄이 통째로 Open-Meteo 것이 되었으므로
> 화면이 그렇게 말해야 한다(`Weather.precipitationSource`) — 숨기면 「폴백으로 Open-Meteo가
> 답했는데 AccuWeather라고 적는」 그 거짓말과 같은 일이다. 일별까지 Open-Meteo가 맡은 날은
> 두 값이 같아 한 줄로 접힌다.
>
> 실측(2026-08-22, 알람 지역 넷): 두 경로 모두 확률과 봉우리가 일치했다 — 미금 92%·서현 96%·
> 잠실 96%·삼성중앙 96%, 각각 토막 셋.
>
> ⚠️ **보충 경로는 실패·빈손·성공을 각각 로그로 남긴다.** 예전에는 빈손이 조용해서
> 「마른 기간이었다」와 「빈손으로 왔다」가 로그에서 구분되지 않았다 — 신고가 들어와도
> 짚을 단서가 없던 자리다. 그리고 `OpenMeteoHourlyClient`에는 **테스트가 하나도 없었다.**

---

# 관통하는 함정 하나: 긴 TTL 캐시가 고침보다 오래 산다

**고친 코드가 멀쩡한데 답이 안 바뀌는 일이 있다.** `/weather 미금`이 그랬다 — `성남시,
대한민국`이어야 하는데 **`Seongnam, 대한민국`**(전라북도 남원시, 분당에서 200km)이 나왔다.
해석기도 지오코더도 골든 테스트도 전부 맞았다. 틀린 것은 **캐시에 남은 옛 값**이었다:

```
weather-resolve::미금  →  "성남"                                     (7일,  옛 프롬프트)
        ↓
geocode::성남|KR      →  GeoLocation("Seongnam","대한민국",35.54,…)   (30일, 옛 선택 규칙)
```

Redis가 AOF + 이름 붙은 볼륨이라 **재시작해도 안 지워진다.** 그리고 그 내내
`golden/messages.txt`는 `성남시, 대한민국`을 지키며 **초록이었다** — 골든은 우리 픽스처를
렌더하니까 맞고 화면은 캐시를 렌더하니까 틀렸다.

**규칙 둘로 막는다.**

1. **파생 규칙이나 프롬프트를 고치면 캐시 이름의 판 번호를 올린다** — `geocode-v2`처럼
   (`CacheNames`). 올리면 옛 키는 아무도 안 읽고 TTL이 지나 스스로 사라진다. 판을 매기는 것은
   **값이 우리 규칙의 산물인 캐시**(지오코딩·해석기 셋)뿐이다. 시세는 수명이 초·분이라
   스스로 낫는다.
2. **파생된 값을 캐시에 담지 않는다.** 표시 이름을 정하는 판단은 담을 때가 아니라 **읽을 때**
   한다(`GeoLocation.labelledFor`). 그러면 규칙을 고치는 순간 옛 항목까지 함께 낫는다.

> **실물 감사도 더운 캐시에서는 못 본다.** `ARCHITECTURE.md`가 "단위 테스트가 통과해도 이어
> 붙이면 틀릴 수 있다"고 적어 둔 것에 한 줄이 더 붙는다 — **감사는 찬 캐시에서 한다.**
> 이미 나간 판에서 항목 하나만 버려야 할 때는 `DELETE /actuator/evict?name=…&key=…`가 있다
> (Boot의 `/actuator/caches`는 통째로만 비운다).

---

# 출처별 결정과 함정

아래는 전부 **실측으로 확인한 것**이다. 문서가 아니라 실물이 근거다.

## 뉴스

글로벌 점유율 순 다섯: **Yahoo Finance · Investing.com · CNBC · BBC Business · AP News**

**페이월 매체는 쓰지 않는다.** 링크를 눌러도 못 읽는 기사는 답이 아니다. 그래서 블룸버그·
로이터·파이낸셜 타임즈·이코노미스트를 뺐다. 새 매체를 더할 때도 같은 기준이다 — 전문이
무료가 아니면 넣지 않는다. **페이월 우회 도구(archive.ph 등)로 되살리지 않는다.**

> **매체는 다섯인데 피드는 여섯이다.** Investing.com만 본 섹션과 암호화폐 섹션을 함께 단다.
> 코인 기사가 금융 일반 피드에 거의 안 실려서다 — 실측: Yahoo 49건 중 3건, 나머지 넷은 0건.
> 매체를 늘린 것이 아니라 같은 매체 안에서 섹션을 연 것이라 화면 표기도 Investing.com 그대로다.

> ⚠️ **매체를 고르는 것만으로는 부족하다.** 그 매체 피드가 남의 기사를 실어 나른다 —
> Yahoo 피드는 48건 중 8건이 `wsj.com`·`investors.com`이었고 둘 다 페이월이다.
> 그래서 그 매체 피드에서는 **그 매체 기사만** 쓴다(`NewsSource.owns`).

## 환율 — 이중화 3단

| 순위 | 출처 | 성격 | 화면 표기 |
|---|---|---|---|
| 1 | 한국투자증권 | 하루 중에도 움직인다 | 시각까지 (`FxSource.intraday`) |
| 2 | 유럽중앙은행 | 영업일 16시 CET 고시 | 날짜 + `(고시)` |
| 3 | 수출입은행 | 하루 한 번 고시 | 날짜 + `(고시)` |

**순서를 바꾸는 것으로 실시간이 되지 않는다.** 유럽중앙은행은 하루 한 번 낸다.

**유럽중앙은행이 수출입은행보다 앞인 이유는 브리핑 시각(오전 9시)이다.** 그때 받을 수 있는
가장 최근 값이 어제 23시(KST)로, 수출입은행의 어제 11시 고시보다 신선하다. 키도 한도도 없어
받쳐 주는 자리에도 맞다.

> **매번 셋을 다 불러 가장 신선한 것을 고르지 않는다.** 그건 이중화가 아니라 *선택*이고,
> 요청마다 수출입은행 하루 1,000회를 태운다. 폴백은 화면이 출처와 날짜로 밝힌다.

## 주식 — 국내와 미국이 따로

| 시장 | 1순위 | 2순위 |
|---|---|---|
| 국내 | 한국투자증권 *(실시간)* | 공공데이터포털 *(전일 종가)* |
| 미국 | 한국투자증권 | FMP |

교체가 아니라 **1순위 승격**이다.

### FMP 무료 티어의 한계

- **한국 종목을 못 준다** (`005930.KS`가 402). 그래서 국내 폴백은 공공데이터포털이다.
- **미국 종목도 다 주지 않는다.** 심볼별 허용목록이고 규칙이 없다 —
  실측: `AAPL`·`NVDA`·`KO`·`PLTR`·`^IXIC`·`^GSPC`·`^DJI`는 200인데 `PATH`·`ORCL`·`SNOW`는 402다
  (초대형주도 막힌다). 그래서 **미국 종목의 2순위는 있으나 마나 한 자리**이고 1순위(KIS)가
  임의 심볼을 다 맡아야 한다.
- **전망 엔드포인트도 같은 허용목록에 걸린다.** 「걸릴 수 있다」가 아니라 **실측이다**
  (2026-08-21):

  | 심볼 | `price-target-consensus` | `earnings` |
  |---|---|---|
  | `AAPL` | 200 (`targetConsensus` 340.72) | 200 (`2026-10-29`) |
  | `ORCL` | **402** | **402** |
  | `PATH` | **402** | **402** |

  본문이 이유를 그대로 말한다 — `This value set for 'symbol' is not available under your
  current subscription`. **시세가 402인 심볼은 전망도 402다.** 그래도 `outlook()`은
  **하나씩 따로 잡는다** — 둘 중 하나만 열린 심볼이 없다는 보장이 없고, 하나가 죽었다고
  살아 있는 쪽까지 버릴 이유가 없다. 하루 250회를 둘이 나눠 쓰는 제약도 여기서 나온다.

  > ⚠️ **미국 목표가에는 2순위가 없다.** 시세는 `US_ORDER = KIS, FMP`인데 `UsOutlookClient`는
  > 구현이 FMP 하나뿐이다. 그래서 **`/stock 유아이패스`에 목표가 줄이 없는 것이 정상**이고,
  > 무료 대안을 찾기 전에는 코드로 고칠 수 있는 자리가 아니다.
  >
  > ⚠️ **브레이커를 시세와 나눈다**(`fmpOutlook`). 허용목록 밖 심볼의 402는 **늘** 나므로,
  > 한 브레이커면 그것이 쌓여 열리는 순간 **미국 시세의 2순위까지 함께 막힌다** —
  > KIS가 죽었을 때 기대던 폴백이 보충 조회 때문에 끊기는 뒤집힌 모양이다.
  > 리미터는 하루 250회가 한 예산이라 그대로 함께 쓴다.

  `grades-consensus`도 200이었지만 **부르지 않는다** — 그것으로 만들던 투자의견을 걷어냈다.

### 한국투자증권(KIS)의 성질

**종목명 검색이 없다** — 조회가 언제나 코드 → 이름 방향이다. 그래서 이름으로 찾는 경로는
2순위에만 있고, 주 경로는 LLM(`StockResolver`)이 준 종목코드로 부른다.

**조회 키가 남과 다르다.**

| 대상 | 요구하는 키 |
|---|---|
| 국내 지수 | 업종코드 — 코스피 `0001`, 코스닥 `1001` |
| 미국 지수 | 제 심볼 — `^IXIC`가 아니라 `COMP`, `.DJI` |
| 미국 종목 | 거래소 코드 (`EXCD=NAS`) |

**미국 종목의 거래소는 설정이 아니라 KIS가 스스로 찾는다.** NAS로 묻고 비면 NYS, 그래도 비면
AMS로 다시 묻고, 찾으면 30일 기억한다. 빗나간 거래소는 에러가 아니라 `rt_cd=0`에 값만 빈
문자열로 온다.

> ⚠️ **AMS를 뺐다가 넣었다 — 뺐던 이유가 사실이 아니었다.** 예전 근거는 「소형주 거래소라
> 빗나갈 때마다 1초씩 더 태우는 대가가 크고, **거기 있는 종목은 2순위가 맡는다**」였다.
> 둘 다 틀렸다.
>
> **그 2순위가 비어 있다.** FMP 무료 티어는 심볼별 허용목록이라 `ORCL`·`SNOW` 같은 초대형주도
> 402다 — 이 문서가 이미 「미국 종목의 2순위는 있으나 마나 한 자리」라고 적어 두고 있었는데,
> AMS를 뺀 결론만 그 사실을 안 읽었다.
>
> 그리고 **「AMS = 소형주 거래소」도 절반만 맞다.** KIS 분류에서 그 칸은 **NYSE Arca 상장 ETF
> 전체**를 함께 삼킨다. 그래서 `/stock JEPI`·`SCHD`·`SOXL`이 통째로 빈손이었다.
> 실측(2026-08-26, 모의계정 12/12 `rt_cd=0`):
>
> | 심볼 | NAS | NYS | AMS | `rsym` | `etyp_nm` |
> |---|---|---|---|---|---|
> | `AAPL` | 309.90 | (빔) | (빔) | `DNASAAPL` | |
> | `SCHD` | (빔) | (빔) | **35.11** | `DAMSSCHD` | `ETF` |
> | `JEPI` | (빔) | (빔) | **58.14** | `DAMSJEPI` | `ETF` |
> | `SOXL` | (빔) | (빔) | **115.67** | `DAMSSOXL` | `ETF` |
>
> **순서는 NAS → NYS → AMS다.** 흔한 것이 앞이라 평상시 비용은 그대로이고, 늘어나는 것은
> **없는 심볼을 물었을 때의 2초 → 3초**뿐이다. 찾은 거래소는 30일 기억하므로 **반복 검색
> 비용은 0**이다.

> ⚠️ **거래소를 늘리는 것만으로는 안 뚫렸다 — 문이 둘이었다.** `StockResolver` 프롬프트가
> 「**미국 거래소(NASDAQ·NYSE)만** 다룹니다. 그 밖의 시장이면 전부 null」이라고 못 박고 있어,
> 규칙을 지키는 모델은 Arca ETF에 전부 `null`을 냈다. 지금은 **거래소를 가리지 않는다** —
> 실재는 KIS가 확정하므로(4-5) 프롬프트가 시장을 좁힐 이유가 없었다.
>
> 프롬프트가 바뀌었으니 캐시 판도 올렸다(`stock-resolve-v3`). 안 올리면 옛 프롬프트가 만든
> 답이 7일 남는다.
>
> ⚠️ **검색어는 소문자로 내려간다**(`QueryNormalizer`). LLM에 닿는 것은 `jepi`이므로
> 프롬프트가 **「티커는 대문자로」**를 말해야 `JEPI`가 돌아온다.

> **미국에도 「원문 그대로」 마지막 시도가 있다.** 국내는 기회가 셋인데(코드 → 이름 → 원문)
> 미국은 하나뿐이었다 — `usQuote`가 티커가 없으면 그 자리에서 포기했다. 이제 다른 길이 다
> 막히면 **원문이 영문 1~5자면 대문자로 올려 한 번 더 묻는다.** 지어내는 것이 아니라 사용자가
> 친 글자이고 실재는 KIS가 확정한다. 그래서 **Gemini가 죽어도 `/stock SCHD`가 산다.**
>
> ⚠️ **앞세우지 않는다.** `KO`·`SO`처럼 국내 종목명일 수도 있는 짧은 영문이 있어서, 먼저
> 시도하면 이름 검색을 가로챈다.

> ⚠️ **못 찾은 심볼은 `Unsupported`로 던진다.** 거래소를 다 훑어 전부 빈 문자열이면 그것은
> KIS의 장애가 아니라 KIS가 모르는 심볼이다. 일봉 경로는 같은 조건에서 이미 그렇게 하고
> 있었는데 **시세 경로만 평범한 `IllegalStateException`이었고**, 그 탓에 없는 심볼을 몇 번
> 검색하면 `kisStock` 브레이커가 열려 **국내 시세가 전일 종가로 강등되고 미국 시세는 통째로
> 빈손**이 됐다 — ETF가 안 나오던 동안 그것을 검색할수록 나머지까지 무너지고 있었다.

### ⚠️ KIS는 무효 토큰에 401이 아니라 HTTP 500을 준다

**이것이 가장 오래 헤맨 함정이다.** 2026-08-19에 `/stock 유아이패스`·`오라클`·`스노우플레이크`가
빈손이었고 로그에 `InternalServerError`가 찍혀 "KIS의 NYS 조회가 죽었다"로 결론이 났다.
**틀렸다.** 같은 요청을 유효한 토큰으로 보내면 12/12 성공한다
(실측: `PATH` 15.52 · `ORCL` 141.61 · `SNOW` 323.00 · `KO` 88.72).

원인은 토큰이었다. **앱키당 활성 토큰이 하나**이고, 1분 안에 다시 발급하면 뒤의 것이 무효가
되는데 **그 무효 토큰으로 부르면 401이 아니라 500이 온다.** 그래서 "상대 서버 장애"처럼 보인다.
이유는 그 500 **본문에만** 있다.

```json
{"rt_cd":"1","msg1":"유효하지 않은 token 입니다.","msg_cd":"EGW00121"}
```

`rt_cd` 검사(`KisHeaders.verify`)는 여기까지 오지 못한다 — HTTP 상태가 에러면 본문 파싱이 먼저
던져서다. 그래서 **`KisHeaders.reasonOf`가 이 본문에서 이유를 꺼낸다.** 남는 것이
`InternalServerError` 하나였던 동안 진단이 통째로 어긋났다.

**당장은 재발급으로 낫지 않는다 — 6시간 안에는.** 캐시를 비우고 다시 발급받아도 KIS는
**전에 발급한 그 죽은 토큰을 되돌려준다** — 응답의 만료 시각이 요청 시각보다 20분 이르다는
것이 그 증거다(실측). 반대로 **먼저 발급된 토큰은 계속 유효했다.**

> ⚠️ **이것을 한동안 "자동 복구를 넣지 않는다"로 읽었고, 그게 절반만 맞았다.** 조건이
> "6시간 안"인데 결론에서 그 조건이 사라져 **아무도 죽은 토큰을 버리지 않았다.** 그러면
> 토큰이 기록된 만료까지 **최대 24시간** 남고 그 창 동안 KIS 호출이 전부 실패한다.
> 지금은 버리고 **6시간 뒤에 다시 받는다** — 앞당기지는 않는다(알림톡만 한 번 더 가고
> 결과는 같다). 발급 시각을 캐시에 함께 담는 이유가 "언제부터 6시간인가"를 알기 위해서다.

> 그 무효 토큰은 **검증 절차가 만들었다.** 포트 충돌로 앱을 네 번 재시작하고 Redis DB를
> 옮겨 가며(db3→db4→db5) 토큰 캐시를 매번 버려, 1분 1회 제한을 위반했다.
> `KisTokenStore`가 Redis에 캐시하는 것이 "발급 비용" 때문만이 아니라 **정확성** 때문이기도
> 하다는 뜻이다 — 캐시를 버리면 토큰이 조용히 무효가 된다.

**500은 KIS가 영구 실패를 말하는 기본 표현이다.** 무효 토큰만이 아니다 — 실측 2026-08-20에
**잘못된 앱시크릿도 500**이었다(`EGW00304`: `고객식별키(법인 personalSeckey, 개인 appSecret)가
유효하지 않습니다`). 그래서 **KIS에는 5xx 재시도를 걸 수 없다** — 절대 낫지 않는 실패를 세 번
부르고, 그 거절 하나에 KIS가 **2.2~3.5초**를 쓴다(실측, 웜 커넥션 3회).

| `msg_cd` | 뜻 | 우리가 할 것 |
|---|---|---|
| `EGW00121` | 토큰이 무효다 | **버린다.** 6시간 뒤에 다시 발급한다 |
| `EGW00304` | 앱시크릿이 틀렸다 | 사람이 키를 고친다. 토큰은 건드리지 않는다 |
| `EGW00201` | 초당 거래건수를 넘겼다 | **잠시 뒤 다시 하면 된다** — 셋 중 유일하게 일시적이다 |
| `EGW00105` | 앱시크릿을 잘못 실었다 (토큰 발급 403) | 키가 틀린 것이 아닐 수 있다 — **끝의 줄바꿈**부터 뗀다 |

> ⚠️ **`EGW00201`이 그 규칙의 예외다 — 그런데도 재시도를 안 건다.** 실측(2026-08-20)에서
> 이것만은 잠시 뒤 성공했다. 그래도 안 거는 이유는 **간격 문이 재시도를 곱하기 때문**이다:
> 재시도 한 번이 `min-interval`만큼을 더 쓰고, 그것이 `max-wait` 천장에 닿으면 그 조회는
> 값이 아니라 대기 초과로 끝난다. 걸려면 `msg_cd`로 이것만 골라 걸어야 하고
> (`HttpServerErrorException` 통째로는 영구 실패까지 세 번 부른다), 그건 아직 안 했다.

**500을 보면 토큰을 먼저 의심한다.** 거래소 코드나 엔드포인트를 파헤치기 전에
`docker exec … redis-cli GET kis:token`으로 지금 토큰이 유효한지 확인하는 것이 빠르다.

> **이제는 스스로 빠져나온다.** 예전에는 아무도 죽은 토큰을 버리지 않아 **기록된 만료까지
> 최대 24시간** 남았고, 그 창 동안 KIS 호출이 전부 실패했다 — 미국 종목은 2순위(FMP)가
> `PATH`를 402로 막으므로 `/stock 유아이패스`가 통째로 빈손이었다. 그것이 "고쳤는데 여전히
> 빈손"의 정체다. `KisHeaders.isInvalidToken`이 `EGW00121`을 알아보고
> `KisTokenStore.invalidate()`가 버린다.

### ⚠️ 초당 한도는 "1초에 몇 건"이 아니라 "호출 사이 얼마"다

**resilience4j 리미터로는 지킬 수 없는 제약이다.** `limitForPeriod: 1` / `limitRefreshPeriod: 1s`로
잡아도 그건 **고정 윈도**라서, 첫 호출이 윈도 끝에서 퍼밋을 쓰면 두 번째 퍼밋이 **다음 윈도가
시작되는 순간**에 나온다. 실측으로 미국 종목의 두 거래소 조회(NAS → NYS)가 **120ms 간격**으로
나갔고 KIS가 두 번째를 거절했다.

같은 요청에서 커넥션과 간격만 바꿔 잰 것(2026-08-19, 모의 계정):

| 커넥션 | 간격 | 결과 |
|---|---|---|
| 매번 새로 | 0.05초 | 둘 다 200 |
| **재사용** | 0.2초 | 두 번째가 **초당 거래건수 초과**(HTTP 500) |
| 재사용 | 1초 | 셋 다 200 |

앱은 커넥션 풀을 쓰므로 **언제나 가운데 줄**이다. 그래서 `curl`로는 재현되지 않고 앱에서만
실패했다 — curl은 호출마다 새 커넥션을 연다. 이것이 "코드로는 문제 없어 보이는데 앱만
안 되는" 상태의 원인이었다.

**그래서 `KisThrottle`이 간격 자체를 지킨다**(`market.kis.min-interval`, 기본 1초). 커넥션을
매번 새로 열어 피할 수도 있지만, KIS가 문서로 약속한 규칙은 "초당 1건"이므로 그쪽을 지킨다 —
커넥션마다 센다는 것은 우리가 관찰한 구현 사정일 뿐이다.

> 브리핑은 KIS를 9번 연달아 부르므로 마지막 호출이 ~8초를 기다린다. 늦어진 것이 아니라
> **원래 지불해야 했던 값**이다.

### 값을 잘못 읽게 되는 자리 넷

> ⚠️ **KIS 응답의 `0`은 값이 아니다.** 지수 심볼이 틀리면 에러가 아니라 `0.00`이 온다
> (`DJI`·`DJIA` 실측). `null`만 보면 화면에 지수 0이 찍히고 폴백도 안 일어난다.

> ⚠️ **KIS 국내 지수는 이름을 화면에 쓸 수 없다.** 코스피가 `종합`, 코스닥이 `KOSDAQ`으로 온다.
> 등락률 필드 이름도 종목과 다르다(`bstp_nmix_prdy_ctrt`) — 종목 이름으로 읽으면 값은 멀쩡한데
> 등락률만 조용히 사라진다.

> ⚠️ **미국 종목에는 달러 등락률 필드가 없다.** `t_xrat`은 원화 환산가 기준이라 쓰면 틀린 값이
> 나간다. `last`와 `base`로 직접 낸다.

> ⚠️ **지수 표와 브리핑 목록은 따로 둔다** (`market.kis.us-indices` ↔ `digest.us-symbols`).
> 겸하게 뒀더니 표에 없는 심볼을 KIS가 통째로 거절해 `/stock 유아이패스`·`오라클`이 빈손이 됐다.
> 목록은 "브리핑에 넣을 것", 표는 "KIS가 아는 이름"이다.

### 화면에서 무리를 가르는 법

**무리는 지역(`StockQuote.Market`)으로 가른다.** `realtime`으로 가르던 때가 있었는데 국내가
전일 종가뿐이던 시절에만 맞는 가정이었다 — 국내에 실시간이 붙으면 삼성전자가 「미국」에 찍힌다.

**한 무리에 실시간과 전일 종가가 섞이면**(지수만 성공하고 종목은 폴백) 꼬리에 기준을 성격마다
한 줄씩 쌓는다. **값 줄에는 꼬리표를 붙이지 않는다.**

## 날씨

### 지오코딩 — 국내에서 인구 정렬이 무력했다

지오코딩은 한국어 짧은 지명에 **인구 정보가 없는 마을만** 물어 온다. 그래서 인구로 고르는
규칙이 국내에서는 언제나 무력했고 API 임의 순서의 첫 줄을 집었다.

| 물어본 것 | 예전 결과 |
|---|---|
| 서현 | 김포시 |
| 성남 | 전라북도 (분당에서 200km) |
| 부산 | 경상북도 |
| 서울 | 0건 |

**그래서 둘을 함께 건다**: LLM이 정식 행정명으로 옮기고(서울특별시·성남시·부산광역시),
**인구가 붙은 후보가 하나도 없으면 고르지 않고 빈손을 준다.** 틀린 좌표가 빈손보다 나쁘다.

> **해외는 한국어 통용명을 그대로 둔다**(파리·도쿄·뉴욕). 영문으로 적을 때는 도시 이름을
> 끝까지 적는다 — `New York`은 네브래스카 York(인구 7,864)로 걸린다. `New York City`여야 한다.

> **지명이 로마자로 오면**(`language=ko`인데도 `Jejudo`로 온다) 물어본 한국어 지명으로 적는다.
> 실재와 좌표는 지오코딩이 확정하고 바뀌는 것은 표기뿐이다.

### 알람(6-1)은 지오코딩을 아예 타지 않는다

**지오코딩이 역 이름을 못 찾는다** — `서현`을 물으면 김포시 서현이 1위다. 그래서 알람 지역은
**좌표를 설정에 직접 박는다.** 덤으로 이 경로는 지오코딩도 LLM도 안 탄다.

> ⚠️ **슬롯 키에 `weather-` 접두사를 붙인다.** `SendHistory`가 브리핑과 같은 저장소를 쓰고 둘 다
> 슬롯이 `yyyy-MM-dd`라, 안 붙이면 **6시 날씨가 슬롯을 잡아 9시 브리핑이 통째로 안 나간다.**

### 검색(6-2)은 기간도 말로 받는다

`내일 서현` · `2025년 8월 19일 서현` · `일주일치 파리`.

`/stock`·`/crypto`와 같이 LLM이 검색어를 보정하되 **좌표는 지어내게 두지 않는다** — LLM은
지명·기간만 내놓고 실재 여부와 좌표는 Open-Meteo 지오코딩이 확정한다.

> ⚠️ **상대 표현(`내일`)을 LLM이 날짜로 계산하게 두지 않는다.** `offsetDays`로 받아 코드가 그날
> 편다 — 굳혀 받으면 7일 캐시에 남아 **내일이 영영 그 날짜가 된다.**

**날짜는 조회한 지역의 현지 달력으로 자른다**(`timezone=auto`). KST로 자르면 남의 하루가 쪼개진다.

### 날씨 이중화 — AccuWeather → Open-Meteo

**1순위만 API 키를 쓴다**(`ACCU_API_KEY`). 무료 등급이 하루 50회이고 일일예보는 5일까지라,
받쳐 주는 쪽은 키가 없어 한도에 안 걸리는 Open-Meteo다 — **제약이 적은 쪽이 뒤에 서야 한다.**

- AccuWeather는 좌표로 바로 못 부른다. 지점 키를 먼저 받아야 해서 조회 한 번이 호출 두 번인데,
  지점 키를 30일 캐시(`accu-location`)해 조회당 1회로 낮춘 것이 **한도의 실질 방어**다.
- **5일을 넘는 기간**(예: `일주일치 파리`)은 `supports()`에서 빠져 Open-Meteo가 맡는다 —
  못 하는 출처는 부르지 않는다. 그래야 브레이커도 한도도 헛되이 안 쓴다.
- **지난 날짜**는 Open-Meteo Archive 단일 경로다(다른 곳에는 아카이브가 없다).
- **지오코딩도 단일이다.** 이중화되는 것은 예보다.

> **강수는 출처가 주는 것을 제 이름으로 적는다** — 예보는 `강수확률 %`, 지나간 날은 `강수량 mm`.
> 예보 두 곳 모두 확률을 주므로 폴백해도 표기가 낮아지지 않는다.
> 단 AccuWeather는 `details=true`가 없으면 확률이 아예 안 온다 — 빼면 조용히 mm로 강등된다.
> **평상시 경로를 폴백에 맞춰 낮추지 않는다.** 이중화는 장애 대비이지 표기를 맞추는 일이 아니다.

> AccuWeather 키가 없거나 한도(503)를 넘으면 그 조회는 실패로 잡혀 Open-Meteo가 답한다.
> **키를 안 넣어도 날씨는 나간다** — 대신 매 조회가 헛호출을 한 번 태운다.
