시니어 개발

뉴스 매체 — Yahoo Finance, Investing.com, CNBC, BBC Business, AP News (글로벌 점유율 순 5곳)
※ 페이월 매체는 쓰지 않는다. 링크를 눌러도 못 읽는 기사는 답이 아니다.
   그래서 볼륨버그·로이터·파이낸셜 타임즈·이코노미스트를 뺐다.
   새 매체를 더할 때도 같은 기준이다 — 전문이 무료가 아니면 넣지 않는다.
   페이월 우회 도구(archive.ph 등)로 되살리지 않는다.
※ 매체는 다섯이지만 피드는 여섯이다 — Investing.com만 본 섹션과 암호화폐 섹션을 함께 단다.
   코인 기사가 금융 일반 피드에 거의 안 실려서다(실측: Yahoo 49건 중 3건, 나머지 넷은 0건).
   매체를 늘린 것이 아니라 같은 매체 안에서 섹션을 연 것이라 화면 표기도 Investing.com 그대로다.
※ 매체를 고르는 것만으로는 부족하다 — 그 매체 피드가 남의 기사를 실어 나른다.
   Yahoo 피드는 48건 중 8건이 wsj.com·investors.com이었고 둘 다 페이월이다.
   그래서 그 매체 피드에서는 그 매체 기사만 쓴다(NewsSource.owns).
텔레그램앱 및 웹사이트

아키텍처
Oracle Cloud Always Free
Github, Github Action
Spring Boot 4
k8s
Redis

HA 구성
- 이중화
- 서킷브레이커
- 캐시
- 락

기능
0-1. API 중심으로 Vuejs로 프론트엔드도 제공하며 Cloud Pages로 프론트엔드 배포

0-2. 구글 에드센스도 붙임

1-1. 텔래그램으로 오전 9시마다 아래 항목 제공
- 환율
- 유명 주식 가격
- 유명 코인 가격
- 재테크에 있어서 꼭 필요한 뉴스를 뽑아 각각 1개(한글 요약)


2. /news ${검색어} 명령어 입력시 검색어에 대응되는 조회수 또는 댓글 순위 1위 뉴스 제공(한글 5줄 요약 동일하게 제공)
3. /fx 명령어 입력시 환율(KRW-USD) 반환. .env에 API 비밀키들 작성해놓음, 이거 절대 노출되면 안됨
※ 이중화는 신선한 순서다 — 한국투자증권(1순위) → 유럽중앙은행(2순위) → 수출입은행(3순위).
   KIS만 하루 중에도 움직여서 시각까지 찍는다(FxSource.intraday). 나머지 둘은 하루 한 번
   고시라 날짜와 '(고시)'로 끝맺는다. 순서를 바꾸는 것으로 실시간이 되지 않는다 —
   유럽중앙은행은 영업일 16시 CET에 한 번 낸다.
※ 그 둘 사이에서 유럽중앙은행이 앞인 이유는 브리핑 시각(오전 9시)이다. 그때 받을 수 있는
   가장 최근 값이 어제 23시(KST)로, 수출입은행의 어제 11시 고시보다 신선하다. 키도 한도도
   없어 받쳐 주는 자리에도 맞다.
※ 매번 셋을 다 불러 가장 신선한 것을 고르지 않는다. 그건 이중화가 아니라 선택이고,
   요청마다 수출입은행 하루 1,000회를 태운다. 폴백은 화면이 출처와 날짜로 밝힌다.

4. /stock ${검색어} 명령어 입력시 검색어에 대응되는 현재 주식 가격 정보 제공.
   .env에 API 비밀키들 작성해놓음, 이거 절대 노출되면 안됨
※ 이중화는 국내와 미국이 따로다 — 국내는 한국투자증권(1순위) → 공공데이터포털(2순위),
   미국은 한국투자증권(1순위) → FMP(2순위). 교체가 아니라 1순위 승격이다.
※ FMP 무료 티어는 한국 종목을 못 준다(005930.KS가 402). 그래서 국내 폴백은 공공데이터포털이다.
※ KIS에는 종목명 검색이 없다 — 조회가 언제나 코드 → 이름 방향이다. 그래서 이름으로 찾는
   경로는 2순위에만 있고, 주 경로는 LLM(StockResolver)이 준 종목코드로 부른다.
※ 무리는 지역(StockQuote.Market)으로 가른다. realtime으로 가르던 때가 있었는데 국내가
   전일 종가뿐이던 시절에만 맞는 가정이었다 — 국내에 실시간이 붙으면 삼성전자가 「미국」에 찍힌다.
※ 한 무리에 실시간과 전일 종가가 섞이면(지수만 성공하고 종목은 폴백) 꼬리에 기준을 성격마다
   한 줄씩 쌓는다. 값 줄에는 꼬리표를 붙이지 않는다.
5. /crypto ${검색어} 명령어 입력시 검색어에 대응되는 현재 코인 가격 정보 제공, 거래소는 업비트

6-1. 날씨 알람 — 매일 오전 6시, 분당 미금역·서현역·잠실역·삼성중앙역
※ 지오코딩이 역 이름을 못 찾는다('서현'을 물으면 김포시 서현이 1위다).
   그래서 알람 지역은 좌표를 설정에 직접 박는다 — 덤으로 이 경로는 지오코딩도 LLM도 안 탄다.
※ 슬롯 키에 'weather-' 접두사를 붙인다. SendHistory가 브리핑과 같은 저장소를 쓰고
   둘 다 슬롯이 yyyy-MM-dd라, 안 붙이면 6시 날씨가 슬롯을 잡아 9시 브리핑이 통째로 안 나간다.

6-2. /weather ${검색어} — 전 세계 모든 지역, 답은 언제나 일일 예보
※ 기간도 말로 받는다 — '내일 서현', '2025년 8월 19일 서현', '일주일치 파리'.
   /stock·/crypto와 같이 LLM이 검색어를 보정하되, 좌표는 지어내게 두지 않는다:
   LLM은 지명·기간만 내놓고 실재 여부와 좌표는 Open-Meteo 지오코딩이 확정한다.
※ 상대 표현('내일')을 LLM이 날짜로 계산하게 두지 않는다. offsetDays로 받아 코드가 그날 편다 —
   굳혀 받으면 7일 캐시에 남아 내일이 영영 그 날짜가 된다.
※ 날짜는 조회한 지역의 현지 달력으로 자른다(timezone=auto). KST로 자르면 남의 하루가 쪼개진다.
※ 이중화는 AccuWeather(1순위) → Open-Meteo(2순위).
   1순위만 API 키를 쓴다(ACCU_API_KEY). 무료 등급이 하루 50회이고 일일예보는 5일까지라,
   받쳐 주는 쪽은 키가 없어 한도에 안 걸리는 Open-Meteo다 — 제약이 적은 쪽이 뒤에 서야 한다.
   AccuWeather는 좌표로 바로 못 부른다. 지점 키를 먼저 받아야 해서 조회 한 번이 호출 두 번인데,
   지점 키를 30일 캐시(accu-location)해 조회당 1회로 낮춘 것이 한도의 실질 방어다.
   5일을 넘는 기간(예: '일주일치 파리')은 supports()에서 빠져 Open-Meteo가 맡는다 —
   못 하는 출처는 부르지 않는다. 그래야 브레이커도 한도도 헛되이 안 쓴다.
   지난 날짜는 Open-Meteo Archive 단일 경로다(다른 곳에는 아카이브가 없다).
   지오코딩도 단일이다 — 이중화되는 것은 예보다.
※ 강수는 출처가 주는 것을 제 이름으로 적는다 — 예보는 '강수확률 %', 지나간 날은 '강수량 mm'.
   예보 두 곳 모두 확률을 주므로 폴백해도 표기가 낮아지지 않는다.
   단 AccuWeather는 details=true가 없으면 확률이 아예 안 온다 — 빼면 조용히 mm로 강등된다.
   평상시 경로를 폴백에 맞춰 낮추지 않는다. 이중화는 장애 대비이지 표기를 맞추는 일이 아니다.
※ AccuWeather 키가 없거나 한도(503)를 넘으면 그 조회는 실패로 잡혀 Open-Meteo가 답한다.
   키를 안 넣어도 날씨는 나간다 — 대신 매 조회가 헛호출을 한 번 태운다.

디렉토리 구조
backend
frontend

전제 조건
반드시 테스트를 넘겨야 다음거 진행 가능
외부 API는 Rate Limit, 응답 에러 처리 등 주의해서 연동



